<div class="container">
<h1>Lists#index</h1>
<% @lists.each do |list| %>
<p><%=link_to list.name, list_path(list)%></p>
<% end %>
<%= link_to "Add a new list", new_list_path,  class: "btn btn-warning" %>
</div>
